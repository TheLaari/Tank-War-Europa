using System;
using System.Collections.Generic;
using System.Text;
using Panzer;

namespace Panzer
{
    class Osuma
{
        /*void NukeOsui(Ydinase ammus, Vaunu kohde, Vaunu pelaaja, Vaunu vihollinen)
        {
            ammus.Destroy();
            kohde.HP--;
            if (kohde.HP == 0)
            {
                kohde.Destroy();
                if (kohde == pelaaja)
                {
                    Tank_War_Europa.PelaajaKuolema();//vaihda PelaajaKuolema()
                }
                if (kohde == vihollinen)
                {
                    kohde.Destroy();
                }
                if (kohde = kaupunki)
                {
                    kaupunki.Destroy;
                }
            }
        }*/
    }
}